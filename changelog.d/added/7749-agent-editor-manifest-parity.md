Also adds the registry's runtime tags API `AgentRegistry::update_tags` that keeps `entry.tags`, `manifest.tags` and the tag index in sync under a single entry guard, closing the tag desync #7742 exposed for standalone tag writes; the manifest write path itself keeps its own `replace_manifest_and_retag` fix from #8103 (#7749) (@DaBlitzStein)
